{
  "name": "erp-lost-children-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Frontend del ERP Lost Children — TypeScript + Vite (arquitectura por capas)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.17.9",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
